apiVersion: v1
kind: Service
metadata:
  name: {{ include "conditions-number.fullname" . }}
  labels:
    {{- include "conditions-number.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "conditions-number.selectorLabels" . | nindent 4 }}
